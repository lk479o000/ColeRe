spring:
    application:
        name: testl_service
    main:
        allow-bean-definition-overriding: true
    cloud:
        nacos:
            server-addr: ${NACOS_SERVER_ADDR}
            username: ${NACOS_USERNAME}
            password: ${NACOS_PASSWORD}
            config:
                namespace: ${NACOS_CONFIG_NAMESPACE:}
                file-extension: yaml
                enabled: true
                shared-configs:
                    -   dataId: environment-properties.yaml
                        refresh: true
                    -   dataId: feign-servers.yaml
                    -   dataId: common-conf.yaml